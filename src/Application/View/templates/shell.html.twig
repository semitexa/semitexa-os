<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Semitexa OS</title>
    <link rel="stylesheet" href="{{ asset('semitexa-os-shell/tokens.css', 'skins') }}">
    <link rel="stylesheet" href="{{ asset('css/design-extras.css', 'os') }}">
    <link rel="stylesheet" href="{{ asset('css/shell.css', 'os') }}">
    <script src="{{ asset('js/vendor/lucide.min.js', 'os') }}"></script>
    <script src="{{ asset('js/vendor/force-graph.min.js', 'os') }}"></script>
</head>
<body>
    <div class="os-root" id="root"></div>
    <div id="wm-layer" hidden></div>
    <div id="weave-layer" hidden></div>

    <script type="application/json" id="os-boot">{{ { providerName: providerName|default('llm'), providerModel: providerModel|default('?'), providerHealthy: providerHealthy, skills: skills|default([]), assistantName: assistantName|default('Solomiia'), userName: userName|default(''), windowMode: windowMode|default('web'), bridgeUrl: bridgeUrl|default('http://127.0.0.1:8777'), locale: locale|default('en'), strings: strings|default({}), inputLayouts: inputLayouts|default(null) }|json_encode|raw }}</script>
    {# ui-core is an ESM+IIFE dual: as a module it populates window.SemitexaUi.core
       (openFeedChannel — the shared KISS live-feed transport). Module scripts and
       the deferred shell.js run in document order, so core lands first; shell.js
       still guards the global and falls back to polling when it is absent. #}
    <script type="module" src="{{ asset('js/ui-core.js', 'platform-ui') }}"></script>
    <script src="{{ asset('js/shell.js', 'os') }}" defer></script>
</body>
</html>
